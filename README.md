# QA Scanner

TypeScript CLI scaffold for a local QA scanner take-home assignment.

## Sample Report Note

The final deliverable must generate `reports/sample-report.md` by running the scanner against `examples/demo-project`.

The sample report must be produced by the scanner and must not be manually written. During TODO #2, the scanner intentionally writes the temporary report `reports/todo2-report.md` instead.
